# Backend API base URL — must be a full URL (e.g. http://localhost:8000), not just ":8000".
# Use the SAME host as your app (e.g. if you open http://localhost:3000, use http://localhost:8000)
# so the session cookie is sent when calling the API.
NEXT_PUBLIC_API_URL=http://localhost:8000
